import React from 'react';
import { Login } from '../../screens/Auth';
import Layout from '../../components/Marketing/Layout';

const Auth = () => {
  return <Login />;
};

Auth.Layout = Layout;

export default Auth;
